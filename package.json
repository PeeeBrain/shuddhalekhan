{
  "name": "shuddhalekhan-monorepo",
  "private": true,
  "version": "0.0.0-development",
  "workspaces": [
    "app",
    "web"
  ],
  "scripts": {
    "dev": "bun run dev:app",
    "dev:app": "bun run --cwd app dev",
    "dev:web": "bun run --cwd web dev",
    "typecheck": "bun run --cwd app typecheck && bun run --cwd web typecheck",
    "lint": "bun run --cwd app lint && bun run --cwd web lint",
    "lint:fix": "bun run --cwd app lint:fix && bun run --cwd web lint:fix",
    "test": "bun run --cwd app test",
    "prototype:agent-boundary": "bun run --cwd app prototype:agent-boundary"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "bun@1.3.14"
}
